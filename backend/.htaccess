RewriteEngine On
RewriteBase /

# If the file exists on disk, serve it directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, proxy to Node.js
RewriteRule ^(.*)$ http://127.0.0.1:5000/$1 [P,L]
