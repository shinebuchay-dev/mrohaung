<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Exclude deploy.php and other files from proxy
  RewriteCond %{REQUEST_URI} !^/deploy\.php$
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ http://127.0.0.1:5000/$1 [P,L]
</IfModule>
