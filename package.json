{
    "name": "mrohaung-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd web && npm install && npm run build",
        "start": "cd web && npm start",
        "dev": "cd web && npm run dev"
    },
    "dependencies": {
        "next": "latest",
        "express": "latest"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}